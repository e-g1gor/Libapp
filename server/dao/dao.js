'use strict'

// Data access object

const mysql = require('mysql2')


/** db connection handler */
let connection = {}


/** DAO implementation */
module.exports = class DAO {
  constructor(connect) {
    // set connection
    // if(connect) connection = .....
  }
}